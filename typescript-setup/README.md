## Simple express app with typescript

> Made a simple configs with TS for a workflow

- Install dependencies `npm install`;
- Run dev version `npm run dev`;
- Build app `npm run build`;
- Run prod app `npm start`;